dependencies:
  post:
    - cd review_front && bower install && npm install
test:
  override:
    - ./bin/rake spec
    - ./node_modules/.bin/testem ci
    - ./bin/rake ember:test
deployment:
  master:
    branch: master
    commands:
      - bundle exec cap staging deploy
