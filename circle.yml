dependencies:
  pre:
    - 'npm install testem@0.6.5'
test:
  post:
    - './node_modules/.bin/testem ci'
deployment:
  master:
    branch: master
    commands:
      - bundle exec cap staging deploy
